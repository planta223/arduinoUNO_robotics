socket_send_string(CMD_DOOR_STATUS)
socket_send_string("\n")
pc_response = socket_read_line()

if pc_response == RESP_DOOR_OPEN:
  door_state = 1
  fail_flag = 0

elif pc_response == RESP_DOOR_CLOSED:
  door_state = 0
  fail_flag = 1

else:
  fail_flag = 1
end