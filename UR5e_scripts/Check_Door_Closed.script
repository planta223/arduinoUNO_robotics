socket_send_string(CMD_DOOR_STATUS)
global pc_response = socket_read_line()

if pc_response == RESP_DOOR_CLOSED:
  global door_state = 0
  global fail_flag = 0

elif pc_response == RESP_DOOR_OPEN:
  global door_state = 1
  global fail_flag = 1

else:
  global fail_flag = 1
end