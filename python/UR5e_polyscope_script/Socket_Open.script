socket_ok = socket_open("192.168.0.10", 5000)

if socket_ok:
  textmsg("Socket open OK")
  socket_send_string("PING\n")
  res = socket_read_line()
  textmsg("PING response: ", res)
else:
  textmsg("Socket open FAILED")
  halt
end